[package]
name = "kingdom-watchdog"
version = "0.1.0"
edition = "2021"

[[bin]]
name = "kingdom-watchdog"
path = "src/main.rs"

[dependencies]
nix = { version = "0.27", features = ["signal", "process"] }
